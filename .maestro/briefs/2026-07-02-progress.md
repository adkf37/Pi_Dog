# Checkpoint Brief - progress

Project: Pi_Dog
Checkpoint: progress
Return Phase: build
Generated: 2026-07-02

## What Happened Since The Last Checkpoint
- Build Batch 1–3 (tasks 01–05) implemented and verified — scaffold, config, schema (14 actions), policy (bench/allow-list/clamp), mock robot; 21 tests passing.
- Build Batch 4 (task-06, Ollama Client) also completed ahead of schedule — `LLMBase` abstract class, `OllamaClient` with httpx, `LlamaCppClient` stub all committed.
- CLI entry point (`main.py`) and template `__init__.py` stubs for future phases (perception, speech, runtime) created.
- No LLM-specific tests written yet (task-06 acceptance check missing).
- STATUS.md is stale — still lists Batch 4 as the next action despite code being present.

## What Will Be Spent Next
- **Batch 5 — task-07 (Planner):** Implement prompt builder, action parser, plan validator. Depends on schema, policy, and LLM client — all ready. ~1 build session.
- **Batch 6 — task-08 (CLI Demo):** Wire main.py to config + planner + mock robot for end-to-end demo. ~1 build session.
- **Validation:** Add LLM client unit tests (mocked httpx) as a prerequisite before closing Batch 4. ~0.5 validator session.

## Questions

### Q1 - Should task-06 (Ollama Client) be validated before proceeding to task-07 (Planner)?
Question: Task-06 code exists but has no unit tests. The planner depends on the LLM client interface, which is implemented but unverified. Should a validator session add mocked tests for the client before the build agent starts the planner, or should the build agent cover tests while implementing the planner?
Options: Run validator first to add and pass LLM client tests; Let the build agent write client tests alongside the planner
Default: Run validator first to add and pass LLM client tests
Decision: default

### Q2 - Should STATUS.md be updated to reflect actual code state?
Question: STATUS.md lists the next action as Batch 4 (task-06), but the code for task-06 is already committed. Should the scribe update STATUS.md now, or let the next build/validator agent handle it?
Options: Scribe updates STATUS.md immediately; Leave it for the next agent to correct
Default: Scribe updates STATUS.md immediately
Decision: default
